[build-system]
# Minimum requirements for the build system to execute.
requires = [
    "setuptools<60.0",
    "wheel",
    "Cython>=0.29.30,<3.0",

    "matplotlib",
    "meshio",
    "oldest-supported-numpy",
    "pyparsing",
    "scipy",
    "tables",
]
